namespace FinalLab1.Entities;


    public enum Gender { Male, Female, Other }
    public enum TicketStatus { Pending, Confirmed, Cancelled }
    public enum TicketType { Standard, VIP, Group }
    public enum PaymentStatus { Pending, Completed, Failed }
    public enum EventStatus { Upcoming, Ongoing, Finished, Cancelled }
    public enum EventSessionType { Online, Offline }
public enum Role { Admin,User}
